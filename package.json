{
  "name": "create-cloud-app",
  "version": "1.0.0",
  "description": "A script to provision a new app on a cloud server",
  "main": "index.js",
  "scripts": {
    "create": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "path": "^0.12.7"
  }
}
